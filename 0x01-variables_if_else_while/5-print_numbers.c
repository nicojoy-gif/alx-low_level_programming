#include <stdio.h>

/**
* main -prints all single digit of a base 10
* followed by a new line
* Return: Always 0 (Success)
*/
int main(void)
{
	int 1;

	for (1 = 0; 1 < 10; 1 = 1 + 1)
	{
		printf("%d, 1);
	}
	putchar('\n");
	return (0);
}

